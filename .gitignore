# OS
.DS_Store

# partial downloads
*.part

# tool scratch
.playwright-mcp/
__pycache__/
*.pyc

# session scratch — never commit
scratch/

# per-run pipeline state; provenance lives in logs/executed-plan.md
logs/run-status.md

# personal machine-specific Claude Code config — not for a shared repo
.claude/settings.local.json
